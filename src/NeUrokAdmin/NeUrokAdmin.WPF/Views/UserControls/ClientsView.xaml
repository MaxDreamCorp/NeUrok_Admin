<UserControl x:Class="NeUrokAdmin.WPF.Views.UserControls.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeUrokAdmin.WPF.Views.UserControls"
             mc:Ignorable="d" 
             Loaded="UserControl_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="FilterBtnStyle" TargetType="Button" BasedOn="{StaticResource PrimaryBtn}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsFiltering, UpdateSourceTrigger=PropertyChanged}" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource LightYellow}" />
                    <Setter Property="BorderThickness" Value="4" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Margin="0 10">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="AddBtn" 
                        Content="Добавить" 
                        Style="{StaticResource PrimaryBtn}"
                        Click="AddBtn_Click"/>
                <TextBox Width="250" 
                         Text="{Binding QuickSearchText, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource PlaceholderSearch}" 
                         VerticalAlignment="Center" 
                         Margin="10 10"
                         TextChanged="TextBox_TextChanged"/>
                <Button x:Name="FilterBtn" 
                        Style="{StaticResource FilterBtnStyle}" 
                        Content="Фильтр"
                        Click="FilterBtn_Click"/>
            </StackPanel>
            <Button x:Name="ClearBtn" 
                    Content="Очистить" 
                    Style="{StaticResource SecondaryBtn}" 
                    ToolTip="Очистить параметры поиска и вывести весь список" 
                    HorizontalAlignment="Right"
                    Click="ClearBtn_Click"/>
        </DockPanel>
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding DisplayedClients}" 
                  Style="{StaticResource MainDataGridStyle}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False" 
                  IsReadOnly="True"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Resources>
                <Style TargetType="TextBlock" x:Key="WrapStyle">
                    <Setter Property="TextWrapping" Value="Wrap"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" 
                                    Binding="{Binding Id}" 
                                    Width="Auto"/>
                <DataGridTextColumn Header="ФИО Ребенка" 
                                    Binding="{Binding ChildFullname}" 
                                    ElementStyle="{StaticResource WrapStyle}" 
                                    Width="2*"/>
                <DataGridTextColumn Header="Дата рожд." 
                                    Binding="{Binding BirthDate, StringFormat=\{0:dd.MM.yyyy\}}" 
                                    Width="85"/>
                <DataGridTextColumn Header="Класс" 
                                    Binding="{Binding Grade}" 
                                    Width="Auto"/>
                <DataGridTextColumn Header="Статус" 
                                    Binding="{Binding Status.Status}" 
                                    Width="Auto"/>
                <DataGridTextColumn Header="Родитель" 
                                    Binding="{Binding ParentName}" 
                                    Width="*"
                                    ElementStyle="{StaticResource WrapStyle}"/>
                <DataGridTextColumn Header="Телефон" 
                                    Binding="{Binding Phone}" 
                                    ElementStyle="{StaticResource WrapStyle}" 
                                    Width="Auto"/>
                <DataGridTextColumn Header="Желаемые курсы" 
                                    Binding="{Binding WishedCoursesDisplay}" 
                                    ElementStyle="{StaticResource WrapStyle}" 
                                    Width="2*"
                                    MinWidth="125"/>
                <DataGridTextColumn Header="Дата рег." 
                                    Binding="{Binding RegistrationDate, StringFormat=\{0:dd.MM.yyyy\}}" 
                                    Width="80"/>
                <DataGridTextColumn Header="Заметки" 
                                    Binding="{Binding Notes}" 
                                    ElementStyle="{StaticResource WrapStyle}"
                                    Width="2*"/>
                <DataGridTextColumn Header="Доп. телефоны" 
                                    Binding="{Binding AdditionalPhones}" 
                                    ElementStyle="{StaticResource WrapStyle}" 
                                    Width="Auto"
                                    MinWidth="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
